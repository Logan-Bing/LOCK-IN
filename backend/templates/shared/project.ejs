<div class="border p-3">
    <h1><%= project.title %></h1>
    <p><%= project.description %></p>
    <p>user_id = <%= project.user_id %></p>
    <div class="d-flex justify-content-between">
        <a href="/project/<%=project.id %>" class="btn btn-secondary">Aller au projet</a>
        <form action="/project/<%= project.id %>/delete" method="post">
            <button class="btn btn-secondary" style="background-color: red">
                Delete
            </button>
        </form>
    </div>
</div>